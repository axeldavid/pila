#Pila

1. Install Meteor:
   * `$ curl https://install.meteor.com | sh`
2. Clone the repository:
   * `$ git clone https://github.com/axeldavid/pila.git`
3. Run the server:
   * `$ cd pila`
   * `$ meteor`
